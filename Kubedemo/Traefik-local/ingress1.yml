apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: stillton
spec:
  rules:
  - host: stilton.local
    http:
      paths:
      - path: /
        backend:
          serviceName: stilton
          servicePort: http
